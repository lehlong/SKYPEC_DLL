
<input type="hidden" id="hddParent" value="" />
<ul id="treeCostCenter" class="ztree"></ul>

<script type="text/javascript">
    var zNodes = jQuery.parseJSON('@Html.Raw(ViewBag.zNode)');
    $(function () {
        $.fn.zTree.init($("#treeCostCenter"), setting, zNodes);
        var zTree = $.fn.zTree.getZTreeObj("treeCostCenter");
        setTimeout(function () {
            var selected = "@ViewBag.CostCenterSelected";
            if (selected != null && selected !== "") {
                var node = zTree.getNodesByParam("id", selected, null);
                if (node != null) {
                    $("#hddParent").val(node[0].pId);
                    zTree.selectNode(node[0]);
                }
            }
        }, 500);
    });
</script>
